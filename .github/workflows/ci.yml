on:
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Mostrar branches do PR
        run: |
          chmod +x scripts/pr-branches.sh
          scripts/pr-branches.sh

